export const environment = {
  production: true,
  apiUrl: 'http://localhost:5000/api', // Change this to your deployed backend API URL for production (e.g. 'https://api.yourdomain.com/api')
  socketUrl: 'http://localhost:5000' // Change this to your deployed backend socket server URL for production (e.g. 'https://api.yourdomain.com')
};
